(() => {
  // Preencha apenas com valores PUBLICOS do Supabase.
  // Nunca coloque service_role, senha do banco ou qualquer chave secreta neste arquivo.
  window.PH_ANALYTICS_CONFIG = Object.freeze({
    supabaseUrl: '',
    supabasePublishableKey: ''
  });
})();
